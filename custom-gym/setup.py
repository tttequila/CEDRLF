from setuptools import setup

setup(name='MELD',        # Secondary directory
    version='0.1',
    install_requires=['gym', 'gensim', 'numpy', 'pandas', 'scikit-learn']    # And any other dependencies foo needs
)